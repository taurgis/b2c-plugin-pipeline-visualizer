{
  "name": "b2c-plugin-pipeline-visualizer",
  "version": "0.1.0",
  "description": "B2C CLI plugin that exports ASCII pipeline previews from SFCC pipeline XML.",
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "b2c",
    "sfcc",
    "pipeline",
    "ascii",
    "oclif",
    "plugin"
  ],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@oclif/core": "^4.0.0",
    "@xmldom/xmldom": "^0.9.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  },
  "oclif": {
    "commands": "./dist/commands"
  }
}
